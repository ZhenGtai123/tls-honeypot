services:
  honeypot:
    build:
      context: .
      dockerfile: deployments/honeypot.Dockerfile
    expose:
      - "8080"
    networks:
      - honeynet
    volumes:
      - ./logs:/logs
    restart: unless-stopped

  proxy:
    build:
      context: .
      dockerfile: deployments/proxy.Dockerfile
    ports:
      - "8443:8443"
    depends_on:
      - honeypot
    networks:
      - honeynet
    volumes:
      - ./logs:/logs
    command:
      - --listen=:8443
      - --target=honeypot:8080
      - --rotate-cert-interval=24h
      - --log-dir=/logs
    restart: unless-stopped

networks:
  honeynet:
    driver: bridge
